# backend/.env.example — Environment Variables
# ---------------------------------------------------------------------------
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit the .env file — it contains secrets.
# ---------------------------------------------------------------------------

# Google Cloud API key for the Vision API (WEB_DETECTION).
#
# To set this up:
#   1. Go to https://console.cloud.google.com/apis/credentials
#   2. Click "Create Credentials" → "API key"
#   3. (Recommended) Click "Restrict key" → limit to "Cloud Vision API" only
#   4. Make sure the Cloud Vision API is enabled for your project:
#      https://console.cloud.google.com/apis/library/vision.googleapis.com
#   5. Paste your key below
#
GOOGLE_VISION_API_KEY=your-api-key-here

# Brave Search API key (for text claim analysis).
#
# To set this up:
#   1. Go to https://brave.com/search/api/
#   2. Sign up for the Free plan (2,000 queries/month)
#   3. Copy your API key from the dashboard
#
BRAVE_SEARCH_API_KEY=your-brave-api-key-here

# Anthropic API key (Claude — used to evaluate claims).
#
# To set this up:
#   1. Go to https://console.anthropic.com/
#   2. Create an API key
#   3. Paste it below
#
ANTHROPIC_API_KEY=your-anthropic-api-key-here
